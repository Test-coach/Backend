const User = require('./user.schema');
const Course = require('./course.schema');
const Order = require('./order.schema');
const TypingTest = require('./typing-test.schema');
const TestPassage = require('./test-passage.schema');
const Coupon = require('./coupon.schema');

module.exports = {
  User,
  Course,
  Order,
  TypingTest,
  TestPassage,
  Coupon
}; 